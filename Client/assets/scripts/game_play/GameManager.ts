import { _decorator, Component, Enum, Node, Sprite, SystemEvent } from 'cc';
const { ccclass, property } = _decorator;

@ccclass('GameManager')
export class GameManager extends Component {
    start() {
    }

    update(deltaTime: number) {

    }
}

